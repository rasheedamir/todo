package io.as.todo.store;

/**
 * verify the communication paths and interactions between components to detect interface defects
 */
public interface IntegrationTest
{
    // category to mark integration tests
}
