jar {
    baseName = project.name
    version = '1.1.0.RELEASE'
}

dependencies {
    compile group: 'org.springframework.cloud', name: 'spring-cloud-starter-zuul', version: '1.3.0.RELEASE'

    // zuul needs it
    compile group: 'com.google.guava', name: 'guava', version: '21.0'
}